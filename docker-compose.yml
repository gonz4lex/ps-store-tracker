version: '3.9'

services:
  ps_store_tracker:
    build: .
    container_name: ps_store_tracker
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    environment:
      STREAMLIT_SERVER_PORT: 8501
      STREAMLIT_SERVER_HEADLESS: "true"
      STREAMLIT_SERVER_ENABLECORS: "false"
